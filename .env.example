# AI Gateway key for the reference assistant (app/ai-chat).
# On Vercel this is provided automatically via OIDC; locally, copy to .env.local.
# Get one at https://vercel.com/dashboard (AI Gateway).
VERCEL_AI_GATEWAY_API_KEY=
