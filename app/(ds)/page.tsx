import { DSPageLoader } from '@/components/layout/ds-page-loader';

// Home — the Introduction (overview), inside the persisted (ds) DocsShell.
export default function Home() {
  return <DSPageLoader routeKey="" />;
}
