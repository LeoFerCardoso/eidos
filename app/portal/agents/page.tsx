import AgentsCatalog from '@/portal/agents/agents-catalog';

// Forge — Agents catalog route. A top-level portal page (rail item between
// Catalog and Templates) listing every agent on the platform.
export default function PortalAgentsPage() {
  return <AgentsCatalog />;
}
